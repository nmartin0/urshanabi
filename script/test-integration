#!/bin/sh
# script/test-integration: every component's own script/test-integration.
set -u
cd "$(dirname "$0")/.." || exit 1
PATH="$PWD/.tools/bin:$PATH"
export PATH
exec ./script/each-component test-integration
