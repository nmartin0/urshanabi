#!/bin/sh
# conformance/script/test: the suite's own gates. The properties check a
# running system, so they run under script/test-integration; what runs
# here is this language's gates over the suite's code.
set -u
cd "$(dirname "$0")/../.." || exit 1
exec script/pipeline-gates conformance
